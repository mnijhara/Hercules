import React, { useState } from 'react';
import { Bot, MessageSquare, Phone, Video, Mail, Send, Sparkles, UserRound, RotateCcw } from 'lucide-react';

interface WorkspaceAIDemoSectionProps { onOpenAddSlackModal: () => void; }
type Channel = 'slack' | 'whatsapp' | 'gmeet' | 'email';
const channelCopy: Record<Channel, { label: string; icon: React.ReactNode }> = {
  slack: { label: 'Example: Slack', icon: <MessageSquare className="h-4 w-4" /> }, whatsapp: { label: 'Example: WhatsApp', icon: <Phone className="h-4 w-4" /> }, gmeet: { label: 'Example: Google Meet', icon: <Video className="h-4 w-4" /> }, email: { label: 'Example: Email', icon: <Mail className="h-4 w-4" /> },
};
const starterPrompts = ['What people issue should I deal with first this week?', 'Draft a clear follow-up for a candidate who has not responded.', 'Turn our onboarding checklist into a repeatable workflow.'];

export const WorkspaceAIDemoSection: React.FC<WorkspaceAIDemoSectionProps> = ({ onOpenAddSlackModal }) => {
  const [channel, setChannel] = useState<Channel>('slack'); const [replyChannel, setReplyChannel] = useState<Channel | null>(null); const [input, setInput] = useState(''); const [reply, setReply] = useState<string | null>(null); const [isLoading, setIsLoading] = useState(false); const [error, setError] = useState<string | null>(null);
  const askHercules = async (message: string) => { const trimmed = message.trim(); if (!trimmed || isLoading) return; const requestChannel = channel; setInput(''); setError(null); setReply(null); setReplyChannel(null); setIsLoading(true); try { const response = await fetch('/api/concierge', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: trimmed, context: { channel: requestChannel, surface: 'website-demo' } }) }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data?.error || 'Hercules AI is temporarily unavailable.'); setReply(data?.reply || 'Hercules AI returned no response. Please try again.'); setReplyChannel(requestChannel); } catch (err) { setError(err instanceof Error ? err.message : 'Hercules AI is temporarily unavailable.'); } finally { setIsLoading(false); } };
  return (
    <section id="workspace-ai" className="relative border-b border-slate-200 bg-white py-8 sm:py-11">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div className="mx-auto mb-5 max-w-3xl text-center sm:mb-7"><div className="mb-2 inline-flex items-center gap-2 rounded-full border border-sky-200 bg-sky-50 px-3 py-1.5 text-[11px] font-mono font-bold text-sky-800"><Sparkles className="h-3.5 w-3.5 text-sky-600" />WORKFLOW EXAMPLE</div><h2 className="text-3xl font-extrabold leading-[1.08] tracking-tight text-slate-900 sm:text-5xl">See how Hercules handles an HR task.</h2><p className="mt-2.5 text-[15px] leading-6 text-slate-600 sm:text-lg sm:leading-relaxed">Try a real AI request. The channel buttons below are examples of where your team could work with Hercules; nothing is connected until you request setup.</p></div>
        <div className="mx-auto max-w-4xl overflow-hidden rounded-2xl border border-[#2e354a] bg-[#1a1d28] shadow-2xl"><div className="flex items-center justify-between gap-3 border-b border-[#292f44] bg-[#121520] px-3.5 py-2.5 sm:px-4 sm:py-3"><div className="flex items-center gap-2 text-xs font-bold font-mono text-white"><Bot className="h-4 w-4 text-sky-400" /><span>Hercules AI</span><span className="rounded bg-sky-500/20 px-1.5 py-0.5 text-[9px] uppercase text-sky-300">AI workforce</span></div><button type="button" onClick={() => { setReply(null); setReplyChannel(null); setError(null); setInput(''); }} className="p-1.5 text-slate-400 hover:text-white" aria-label="Reset AI demo"><RotateCcw className="h-3.5 w-3.5" /></button></div><div className="flex gap-2 overflow-x-auto border-b border-[#252b3d] bg-[#151824] px-3.5 py-2 sm:px-4" aria-label="Workflow examples">{(Object.keys(channelCopy) as Channel[]).map((item) => <button key={item} type="button" onClick={() => setChannel(item)} aria-pressed={channel === item} className={`flex shrink-0 items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-bold transition-all ${channel === item ? 'bg-sky-500 text-slate-950' : 'border border-[#333e5c] bg-[#1f2638] text-slate-300'}`}>{channelCopy[item].icon}{channelCopy[item].label}</button>)}</div><div className="min-h-[175px] bg-[#1a1d28] p-3.5 sm:min-h-[200px] sm:p-4.5"><div className="flex items-start gap-2.5"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-slate-700"><UserRound className="h-4 w-4 text-slate-300" /></div><div className="flex-1"><div className="mb-0.5 text-xs font-bold text-white">You · Founder</div><div className="text-[11px] text-slate-400">Try a prompt below, or ask your own question.</div></div></div>{reply && <div className="mt-3.5 flex items-start gap-2.5"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border border-sky-400/30 bg-sky-500/20"><Bot className="h-4 w-4 text-sky-400" /></div><div className="flex-1 rounded-xl border border-[#2a3147] bg-[#121520] p-3"><div className="mb-1.5 text-[11px] font-bold text-sky-300">Hercules AI · {channelCopy[replyChannel || channel].label}</div><p className="whitespace-pre-wrap text-xs leading-5 text-slate-200 sm:text-sm">{reply}</p></div></div>}{isLoading && <div className="mt-3.5 text-xs font-mono text-sky-400 animate-pulse" role="status" aria-live="polite">Hercules AI is preparing the response…</div>}{error && <div role="alert" className="mt-3.5 rounded-lg border border-rose-500/30 bg-rose-950/40 p-2.5 text-xs text-rose-200">{error}</div>}{!reply && !isLoading && !error && <div className="mt-3.5 flex flex-wrap gap-1.5">{starterPrompts.map((prompt) => <button key={prompt} type="button" onClick={() => askHercules(prompt)} className="rounded-lg border border-[#333e5c] bg-[#1f2638] px-2.5 py-1.5 text-left text-[11px] text-sky-300 transition-all hover:bg-[#2a344d]">{prompt}</button>)}</div>}</div><form onSubmit={(event) => { event.preventDefault(); void askHercules(input); }} className="flex items-center gap-2 border-t border-[#252b3d] bg-[#131622] p-2.5 sm:p-3"><input value={input} onChange={(event) => setInput(event.target.value)} disabled={isLoading} maxLength={4000} aria-label="Ask Hercules AI" placeholder={`Ask Hercules AI with ${channelCopy[channel].label.toLowerCase()}…`} className="w-full rounded-lg border border-[#2c354d] bg-[#1b2030] px-3.5 py-2.5 text-xs text-white placeholder-slate-500 focus:border-sky-400 focus:outline-none disabled:opacity-60" /><button type="submit" disabled={!input.trim() || isLoading} className="flex shrink-0 items-center gap-1 rounded-lg bg-sky-500 px-3.5 py-2.5 text-xs font-bold text-slate-950 hover:bg-sky-400 disabled:opacity-40"><Send className="h-3.5 w-3.5" /><span className="hidden sm:inline">Ask</span></button></form><div className="flex flex-col gap-2 border-t border-[#252b3d] bg-[#11141f] px-3.5 py-2.5 sm:flex-row sm:items-center sm:justify-between sm:px-4"><span className="text-[10px] text-slate-500">AI prepares · CHRO decides · approved work gets executed</span><button type="button" onClick={onOpenAddSlackModal} className="text-xs font-bold text-sky-300 hover:text-white">Request workspace setup →</button></div></div>
      </div>
    </section>
  );
};
