import React, { useEffect, useState } from 'react';
import { X, MessageSquare, CheckCircle2, ShieldCheck, ArrowRight, Loader2, AlertCircle } from 'lucide-react';
import { useModalFocus } from './useModalFocus';
interface AddSlackModalProps { isOpen: boolean; onClose: () => void; }
export const AddSlackModal: React.FC<AddSlackModalProps> = ({ isOpen, onClose }) => {
  const [step, setStep] = useState<'form' | 'success'>('form'); const [workspaceName, setWorkspaceName] = useState(''); const [founderEmail, setFounderEmail] = useState(''); const [teamSize, setTeamSize] = useState('10-25 Employees'); const [channels, setChannels] = useState<string[]>(['Slack', 'WhatsApp', 'Google Meet', 'Gmail / Email']); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(''); const dialogRef = useModalFocus(isOpen, onClose);
  useEffect(() => { if (!isOpen) return; setStep('form'); setError(''); setSubmitting(false); setWorkspaceName(''); setFounderEmail(''); setTeamSize('10-25 Employees'); setChannels(['Slack', 'WhatsApp', 'Google Meet', 'Gmail / Email']); }, [isOpen]);
  if (!isOpen) return null;
  const toggleChannel = (channel: string) => setChannels((current) => current.includes(channel) ? current.filter((item) => item !== channel) : [...current, channel]);
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setError(''); const cleanWorkspaceName = workspaceName.trim(); const cleanFounderEmail = founderEmail.trim(); if (!cleanWorkspaceName) { setError('Enter your company or startup name.'); return; } if (!cleanFounderEmail) { setError('Enter your work email.'); return; } if (channels.length === 0) { setError('Select at least one workspace channel.'); return; } setSubmitting(true); try { const response = await fetch('/api/leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: cleanWorkspaceName, company: cleanWorkspaceName, email: cleanFounderEmail, teamSize, notes: `Requested workspace setup for: ${channels.join(', ')}`, source: 'workspace-deployment-request' }) }); if (!response.ok) { const data = await response.json().catch(() => ({})); throw new Error(data?.error || 'We could not submit the setup request.'); } setWorkspaceName(cleanWorkspaceName); setFounderEmail(cleanFounderEmail); setStep('success'); } catch (err) { setError(err instanceof Error ? err.message : 'We could not submit the setup request. Please try again.'); } finally { setSubmitting(false); } };
  const handleClose = () => { setStep('form'); setError(''); setSubmitting(false); setWorkspaceName(''); setFounderEmail(''); setTeamSize('10-25 Employees'); setChannels(['Slack', 'WhatsApp', 'Google Meet', 'Gmail / Email']); onClose(); };
  return <div className="fixed inset-0 z-50 flex items-center justify-center overflow-y-auto bg-slate-900/60 p-3 backdrop-blur-md sm:p-4" role="dialog" aria-modal="true" aria-labelledby="hercules-workspace-title"><div ref={dialogRef} tabIndex={-1} className="relative my-4 w-full max-w-lg rounded-2xl border border-slate-200/90 bg-white p-5 shadow-2xl outline-none sm:my-8 sm:rounded-3xl sm:p-8"><button type="button" onClick={handleClose} aria-label="Close workspace setup form" className="absolute right-4 top-4 rounded-full bg-slate-100 p-2 text-slate-500 hover:bg-slate-200 hover:text-slate-900 sm:right-6 sm:top-6"><X className="h-5 w-5" /></button>{step === 'form' ? <div><div className="mb-5 pr-8"><div className="mb-2 inline-flex items-center gap-2 rounded-full border border-sky-200 bg-sky-50 px-3 py-1 text-[10px] font-mono font-bold uppercase tracking-widest text-sky-800"><MessageSquare className="h-3.5 w-3.5 text-sky-600" />WORKSPACE SETUP REQUEST</div><h3 id="hercules-workspace-title" className="text-xl font-extrabold text-slate-900 sm:text-2xl">Put Hercules in your workspace</h3><p className="mt-1 text-[11px] text-slate-600 sm:text-xs">Tell us where your team works. We will coordinate the real integration and onboarding with you.</p></div><form onSubmit={handleSubmit} className="space-y-3"><div><label htmlFor="workspace-company" className="mb-1 block text-[10px] font-mono font-bold uppercase tracking-wider text-slate-700">Company / Startup Name *</label><input id="workspace-company" type="text" required maxLength={160} autoComplete="organization" placeholder="Your company" value={workspaceName} onChange={(e) => setWorkspaceName(e.target.value)} className="w-full rounded-lg border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-xs text-slate-900 placeholder-slate-400 focus:border-sky-500 focus:outline-none" /></div><div><label htmlFor="workspace-email" className="mb-1 block text-[10px] font-mono font-bold uppercase tracking-wider text-slate-700">Founder Work Email *</label><input id="workspace-email" type="email" required maxLength={254} autoComplete="email" placeholder="you@company.com" value={founderEmail} onChange={(e) => setFounderEmail(e.target.value)} className="w-full rounded-lg border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-xs text-slate-900 placeholder-slate-400 focus:border-sky-500 focus:outline-none" /></div><div><label htmlFor="workspace-team-size" className="mb-1 block text-[10px] font-mono font-bold uppercase tracking-wider text-slate-700">Team size</label><select id="workspace-team-size" value={teamSize} onChange={(e) => setTeamSize(e.target.value)} className="w-full rounded-lg border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-xs text-slate-900 focus:border-sky-500 focus:outline-none">{['1-10 Employees', '10-25 Employees', '25-50 Employees', '50+ Employees'].map((size) => <option key={size}>{size}</option>)}</select></div><fieldset><legend className="mb-1 block text-[10px] font-mono font-bold uppercase tracking-wider text-slate-700">Where should Hercules work?</legend><div className="grid grid-cols-2 gap-1.5 text-[11px]">{['Slack', 'WhatsApp', 'Google Meet', 'Gmail / Email'].map((channel) => <label key={channel} className="flex cursor-pointer items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 p-2 text-slate-800 hover:border-sky-400"><input type="checkbox" checked={channels.includes(channel)} onChange={() => toggleChannel(channel)} className="accent-sky-600" /><span>{channel}</span></label>)}</div></fieldset>{error && <div role="alert" className="flex items-start gap-2 rounded-lg border border-rose-200 bg-rose-50 px-3 py-2.5 text-xs text-rose-700"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0" /><span>{error}</span></div>}<button type="submit" disabled={submitting} className="flex w-full items-center justify-center gap-2 rounded-lg bg-slate-900 py-3 text-xs font-extrabold uppercase tracking-wider text-white shadow-md transition hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-60">{submitting ? <Loader2 className="h-4 w-4 animate-spin text-sky-400" /> : <MessageSquare className="h-4 w-4 text-sky-400" />}<span>{submitting ? 'Sending request…' : 'Request workspace setup'}</span>{!submitting && <ArrowRight className="h-4 w-4" />}</button><div className="flex items-center justify-center gap-1.5 text-[9px] text-slate-500 font-mono"><ShieldCheck className="h-3.5 w-3.5 text-emerald-600" /><span>We will confirm integration access before connecting any workspace.</span></div></form></div> : <div className="space-y-4 py-4 text-center" role="status" aria-live="polite"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-xl border border-emerald-200 bg-emerald-50 text-emerald-600"><CheckCircle2 className="h-7 w-7" /></div><div><span className="mb-1 block text-[9px] font-bold uppercase tracking-widest text-sky-700">REQUEST RECEIVED</span><h3 className="text-xl font-extrabold text-slate-900">We’ll help set up Hercules</h3><p className="mx-auto mt-1 max-w-sm text-xs text-slate-600">Your request for {workspaceName || 'your workspace'} has been sent. We will confirm the integration steps with {founderEmail} before anything is connected.</p></div><button type="button" onClick={handleClose} className="rounded-lg bg-slate-900 px-7 py-2.5 text-xs font-bold text-white shadow-sm">Return to Hercules</button></div>}</div></div>;
};
