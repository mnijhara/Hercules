import React, { useRef, useState } from 'react';
import { Send, CheckCircle2, AlertCircle, Sparkles } from 'lucide-react';

export const CallbackFormSection: React.FC = () => {
  const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [submitted, setSubmitted] = useState(false); const [error, setError] = useState(''); const [submitting, setSubmitting] = useState(false); const successRef = useRef<HTMLDivElement>(null);
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (submitting) return; const trimmedName = name.trim(); const trimmedEmail = email.trim(); if (!trimmedName || !trimmedEmail) return; setError(''); setSubmitting(true); try { const response = await fetch('/api/leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: trimmedName, email: trimmedEmail, source: 'callback-form' }) }); const data = await response.json().catch(() => ({})); if (!response.ok) { const message = data?.error === 'Lead capture is not configured yet' ? 'Callback requests are temporarily unavailable. Please try again shortly.' : (data?.error || 'Unable to submit the request'); throw new Error(message); } setName(trimmedName); setEmail(trimmedEmail); setSubmitted(true); requestAnimationFrame(() => successRef.current?.focus()); } catch (submitError) { setError(submitError instanceof Error ? submitError.message : 'Unable to submit the request. Please try again.'); } finally { setSubmitting(false); } };
  return <section id="callback-form" className="relative overflow-hidden bg-slate-900 py-8 text-white sm:py-11"><div className="pointer-events-none absolute inset-0 bg-[linear-gradient(to_right,#1e293b80_1px,transparent_1px),linear-gradient(to_bottom,#1e293b80_1px,transparent_1px)] bg-[size:3rem_3rem] opacity-30" /><div className="relative z-10 mx-auto max-w-4xl px-4 text-center sm:px-6 lg:px-8"><div className="mb-2.5 inline-flex items-center gap-2 rounded-full border border-sky-800 bg-sky-950/80 px-3 py-1.5 text-[11px] font-mono font-bold text-sky-400"><Sparkles className="h-3.5 w-3.5" />GET STARTED WITH HERCULES</div><h2 className="text-3xl font-extrabold leading-[1.08] tracking-tight sm:text-5xl">Want Hercules in your workspace?</h2><p className="mx-auto mb-5 mt-2.5 max-w-xl text-[15px] font-normal leading-6 text-slate-400 sm:mb-6 sm:text-lg sm:leading-relaxed">Leave your name and email and we&apos;ll reach out to get you set up across your HR workflows and workspace.</p>{submitted ? <div ref={successRef} tabIndex={-1} role="status" aria-live="polite" className="mx-auto max-w-lg space-y-2.5 rounded-2xl border border-emerald-500/40 bg-slate-800/80 p-5 text-center outline-none focus-visible:ring-2 focus-visible:ring-sky-400"><CheckCircle2 className="mx-auto h-9 w-9 text-emerald-400" /><h3 className="text-lg font-bold text-white">Request received</h3><p className="text-xs leading-5 text-slate-300">Thanks {name}. Your request has been sent to the Hercules team. We&apos;ll follow up at {email}.</p></div> : <form onSubmit={handleSubmit} className="mx-auto max-w-md space-y-2.5"><div className="text-left"><label htmlFor="callback-name" className="mb-1 block text-xs font-semibold text-slate-300">Name</label><input id="callback-name" type="text" required autoComplete="name" value={name} onChange={(e) => setName(e.target.value)} placeholder="Your name" className="w-full rounded-xl border border-slate-700 bg-slate-800/90 px-4 py-3 text-sm text-white placeholder-slate-500 transition-all focus:border-sky-400 focus:outline-none" /></div><div className="text-left"><label htmlFor="callback-email" className="mb-1 block text-xs font-semibold text-slate-300">Email</label><input id="callback-email" type="email" required autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" className="w-full rounded-xl border border-slate-700 bg-slate-800/90 px-4 py-3 text-sm text-white placeholder-slate-500 transition-all focus:border-sky-400 focus:outline-none" /></div>{error && <div role="alert" className="flex items-start gap-2 rounded-xl border border-rose-500/40 bg-rose-950/40 px-3 py-2.5 text-left text-xs text-rose-200"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0" /><span>{error}</span></div>}<button type="submit" disabled={submitting} className="flex w-full items-center justify-center gap-2 rounded-xl bg-sky-500 py-3.5 text-sm font-extrabold uppercase tracking-wider text-slate-950 shadow-xl shadow-sky-500/20 transition-all hover:bg-sky-400 disabled:cursor-not-allowed disabled:opacity-60"><span>{submitting ? 'Sending…' : 'Request a callback'}</span><Send className="h-4 w-4" /></button></form>}</div></section>;
};
