import React, { useEffect, useRef, useState } from 'react';
import { X, CheckCircle2, UserCheck, Send, Loader2, AlertCircle } from 'lucide-react';
import { useModalFocus } from './useModalFocus';
interface BookChroModalProps { isOpen: boolean; onClose: () => void; }
export const BookChroModal: React.FC<BookChroModalProps> = ({ isOpen, onClose }) => {
  const [selectedTopic, setSelectedTopic] = useState('Org Design & Career Bands'); const [teamSize, setTeamSize] = useState('10 – 30 people'); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [phone, setPhone] = useState(''); const [notes, setNotes] = useState(''); const [submitted, setSubmitted] = useState(false); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(''); const successRef = useRef<HTMLDivElement>(null); const dialogRef = useModalFocus(isOpen, onClose);
  useEffect(() => { if (!isOpen) return; setSubmitted(false); setError(''); setSubmitting(false); setSelectedTopic('Org Design & Career Bands'); setTeamSize('10 – 30 people'); setName(''); setEmail(''); setPhone(''); setNotes(''); }, [isOpen]);
  if (!isOpen) return null;
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setError(''); setSubmitting(true); try { const response = await fetch('/api/leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: name.trim(), email: email.trim(), phone: phone.trim(), topic: selectedTopic, teamSize, notes: notes.trim(), source: 'fractional-chro-booking' }) }); if (!response.ok) { const data = await response.json().catch(() => ({})); throw new Error(data?.error || 'We could not submit your request.'); } setName(name.trim()); setEmail(email.trim()); setPhone(phone.trim()); setNotes(notes.trim()); setSubmitted(true); requestAnimationFrame(() => successRef.current?.focus()); } catch (err) { setError(err instanceof Error ? err.message : 'We could not submit your request. Please try again.'); } finally { setSubmitting(false); } };
  const handleReset = () => { setSubmitted(false); setError(''); setSubmitting(false); setSelectedTopic('Org Design & Career Bands'); setTeamSize('10 – 30 people'); setName(''); setEmail(''); setPhone(''); setNotes(''); onClose(); };
  const TOPICS = ['Org Design & Career Bands', 'Executive Hiring & Compensation', 'Employee Relations & Manager Support', 'Founder Alignment & People Strategy', 'Full Fractional CHRO Retainer'];
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 p-3 backdrop-blur-md sm:p-4" role="dialog" aria-modal="true" aria-labelledby="hercules-chro-title"><div ref={dialogRef} tabIndex={-1} className="relative my-3 flex max-h-[92vh] w-full max-w-2xl flex-col overflow-hidden rounded-2xl border border-slate-800 bg-slate-900 text-white shadow-2xl outline-none sm:my-8 sm:rounded-3xl"><div className="relative flex shrink-0 items-start justify-between border-b border-slate-800 bg-gradient-to-r from-slate-900 via-slate-850 to-indigo-950 p-5 sm:p-7"><div className="space-y-1 pr-6"><div className="inline-flex items-center gap-2 rounded-full border border-sky-400/30 bg-sky-500/10 px-3 py-1 text-[10px] font-mono font-bold text-sky-400"><UserCheck className="h-3.5 w-3.5" />FRACTIONAL CHRO CONSULTATION</div><h3 id="hercules-chro-title" className="pt-0.5 text-lg font-extrabold sm:text-2xl">Talk to your Fractional CHRO</h3><p className="text-[11px] text-slate-300 sm:text-sm">Tell us what is happening in your people function. We will use it to prepare for the conversation.</p></div><button type="button" onClick={onClose} aria-label="Close booking form" className="shrink-0 rounded-full bg-slate-800 p-2 text-slate-400 hover:bg-slate-700 hover:text-white"><X className="h-5 w-5" /></button></div><div className="space-y-5 overflow-y-auto p-5 sm:space-y-6 sm:p-7">{submitted ? <div ref={successRef} tabIndex={-1} className="space-y-3 rounded-2xl py-7 text-center outline-none focus-visible:ring-2 focus-visible:ring-sky-400" role="status" aria-live="polite"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full border border-emerald-400/40 bg-emerald-500/20 text-emerald-400 shadow-xl"><CheckCircle2 className="h-7 w-7" /></div><h4 className="text-xl font-bold text-white">Request received</h4><p className="mx-auto max-w-md text-xs leading-5 text-slate-300">Thanks <strong className="text-white">{name}</strong>. We have your request about <span className="font-semibold text-sky-400">{selectedTopic}</span>. We will contact <strong className="text-white">{email}</strong> to arrange the next step.</p><p className="mx-auto max-w-md text-[11px] text-slate-400">This form sends your details to the Hercules lead channel. It does not create a calendar booking until the team confirms a time with you.</p><div className="pt-2"><button type="button" onClick={handleReset} className="rounded-full bg-sky-500 px-6 py-2.5 text-xs font-bold uppercase tracking-wider text-slate-950 hover:bg-sky-400">Done</button></div></div> : <form onSubmit={handleSubmit} className="space-y-4"><div><label className="mb-1.5 block text-[11px] font-mono font-bold uppercase tracking-wider text-sky-400">1. What should your CHRO help with?</label><div className="grid grid-cols-1 gap-2 sm:grid-cols-2">{TOPICS.map((topic) => <button key={topic} type="button" onClick={() => setSelectedTopic(topic)} aria-pressed={selectedTopic === topic} className={`flex items-center justify-between rounded-lg border p-2.5 text-left text-[11px] font-semibold transition-all ${selectedTopic === topic ? 'border-sky-400 bg-sky-500/20 text-sky-200 shadow-md' : 'border-slate-700 bg-slate-800/60 text-slate-300 hover:bg-slate-800'}`}><span>{topic}</span>{selectedTopic === topic && <CheckCircle2 className="h-4 w-4 shrink-0 text-sky-400" />}</button>)}</div></div><div><label className="mb-1.5 block text-[11px] font-mono font-bold uppercase tracking-wider text-sky-400">2. Current team size</label><div className="grid grid-cols-2 gap-1.5 sm:grid-cols-4">{['1 – 10 people', '10 – 30 people', '30 – 50 people', '50+ people'].map((size) => <button key={size} type="button" onClick={() => setTeamSize(size)} aria-pressed={teamSize === size} className={`rounded-lg border px-2.5 py-2 text-center text-[11px] font-semibold transition-all ${teamSize === size ? 'border-sky-400 bg-sky-500 text-slate-950 font-extrabold' : 'border-slate-700 bg-slate-800/60 text-slate-300 hover:bg-slate-800'}`}>{size}</button>)}</div></div><div className="space-y-2.5 border-t border-slate-800 pt-3"><label className="block text-[11px] font-mono font-bold uppercase tracking-wider text-sky-400">3. Your details</label><div className="grid grid-cols-1 gap-2.5 sm:grid-cols-2"><div><label className="mb-1 block text-[10px] font-medium text-slate-400" htmlFor="chro-name">Your Name *</label><input id="chro-name" type="text" required autoComplete="name" placeholder="Your name" value={name} onChange={(e) => setName(e.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-xs text-white focus:border-sky-400 focus:outline-none" /></div><div><label className="mb-1 block text-[10px] font-medium text-slate-400" htmlFor="chro-email">Work Email *</label><input id="chro-email" type="email" required autoComplete="email" placeholder="you@company.com" value={email} onChange={(e) => setEmail(e.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-xs text-white focus:border-sky-400 focus:outline-none" /></div></div><div className="grid grid-cols-1 gap-2.5 sm:grid-cols-2"><div><label className="mb-1 block text-[10px] font-medium text-slate-400" htmlFor="chro-phone">Phone / WhatsApp <span className="text-slate-500">(optional)</span></label><input id="chro-phone" type="tel" autoComplete="tel" placeholder="+91 98765 43210" value={phone} onChange={(e) => setPhone(e.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-xs text-white focus:border-sky-400 focus:outline-none" /></div><div><label className="mb-1 block text-[10px] font-medium text-slate-400" htmlFor="chro-notes">Context / goal (optional)</label><input id="chro-notes" type="text" placeholder="What would make the conversation useful?" value={notes} onChange={(e) => setNotes(e.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-xs text-white focus:border-sky-400 focus:outline-none" /></div></div></div>{error && <div role="alert" className="flex items-start gap-2 rounded-lg border border-rose-400/30 bg-rose-500/10 px-3 py-2.5 text-xs text-rose-200"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0" /><span>{error}</span></div>}<div className="flex flex-col items-center justify-between gap-2.5 border-t border-slate-800 pt-3 sm:flex-row"><div className="text-[10px] text-slate-400">Conversation · No commitment</div><button type="submit" disabled={submitting} className="flex w-full items-center justify-center gap-2 rounded-lg bg-gradient-to-r from-sky-400 to-blue-500 px-6 py-2.5 text-[11px] font-extrabold uppercase tracking-wider text-slate-950 shadow-lg shadow-sky-500/20 disabled:cursor-not-allowed disabled:opacity-60 sm:w-auto">{submitting ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <Send className="h-3.5 w-3.5" />}<span>{submitting ? 'Sending…' : 'Request a CHRO conversation'}</span></button></div></form>}</div></div></div>;
};
